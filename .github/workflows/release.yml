name: Release AlgoGenie

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version from tag
        id: get_version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: AlgoGenie ${{ github.ref }}
          body: |
            Nouvelle version d'AlgoGenie

            Telechargement:
            - Linux: Fichier .deb ci-dessous
            - Windows: Fichier .msi ci-dessous

            Installation:

            Linux (Debian/Ubuntu):
            sudo dpkg -i algogenie_*_amd64.deb

            Windows:
            Double-cliquez sur le fichier .msi

            Changements:
            Voir les commits depuis la derniere version
          draft: false
          prerelease: false

  build-release:
    needs: create-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            bundle: deb

          - os: windows-latest
            target: x86_64-pc-windows-msvc
            bundle: msi

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 10

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'pnpm'

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.target }}

    - name: Install Linux dependencies
      if: matrix.os == 'ubuntu-22.04'
      run: |
        sudo apt-get update
        sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

    - name: Cache Rust dependencies
      uses: Swatinem/rust-cache@v2
      with:
        workspaces: src-tauri

    - name: Install dependencies
      run: pnpm install

    - name: Build app
      run: pnpm tauri build --target ${{ matrix.target }}

    - name: Upload Linux DEB
      if: matrix.os == 'ubuntu-22.04'
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ needs.create-release.outputs.upload_url }}
        asset_path: ./src-tauri/target/${{ matrix.target }}/release/bundle/deb/AlgoGénie_${{ needs.create-release.outputs.version }}_amd64.deb
        asset_name: AlgoGenie-${{ needs.create-release.outputs.version }}-linux-amd64.deb
        asset_content_type: application/vnd.debian.binary-package

    - name: Upload Linux RPM
      if: matrix.os == 'ubuntu-22.04'
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ needs.create-release.outputs.upload_url }}
        asset_path: ./src-tauri/target/${{ matrix.target }}/release/bundle/rpm/AlgoGénie-${{ needs.create-release.outputs.version }}-1.x86_64.rpm
        asset_name: AlgoGenie-${{ needs.create-release.outputs.version }}-linux-x86_64.rpm
        asset_content_type: application/x-rpm

    - name: Upload Linux AppImage
      if: matrix.os == 'ubuntu-22.04'
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ needs.create-release.outputs.upload_url }}
        asset_path: ./src-tauri/target/${{ matrix.target }}/release/bundle/appimage/AlgoGénie_${{ needs.create-release.outputs.version }}_amd64.AppImage
        asset_name: AlgoGenie-${{ needs.create-release.outputs.version }}-linux-amd64.AppImage
        asset_content_type: application/x-executable

    - name: Upload Windows MSI
      if: matrix.os == 'windows-latest'
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ needs.create-release.outputs.upload_url }}
        asset_path: ./src-tauri/target/${{ matrix.target }}/release/bundle/msi/AlgoGénie_${{ needs.create-release.outputs.version }}_x64_en-US.msi
        asset_name: AlgoGenie-${{ needs.create-release.outputs.version }}-windows-x64.msi
        asset_content_type: application/x-msi

    - name: Upload Windows NSIS
      if: matrix.os == 'windows-latest'
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ needs.create-release.outputs.upload_url }}
        asset_path: ./src-tauri/target/${{ matrix.target }}/release/bundle/nsis/AlgoGénie_${{ needs.create-release.outputs.version }}_x64-setup.exe
        asset_name: AlgoGenie-${{ needs.create-release.outputs.version }}-windows-x64-setup.exe
        asset_content_type: application/x-msdownload

  update-latest-tag:
    needs: build-release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout release branch
        uses: actions/checkout@v4
        with:
          ref: release

      - name: Update latest tag
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git tag -f latest
          git push -f origin latest
